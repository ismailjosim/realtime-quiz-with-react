import React from 'react';

const Checkbox = () => {
    return (
        <label>
            <input type="checkbox" />
            <span>I agree to the Terms & Conditions</span>
        </label>

    );
};

export default Checkbox;
